Fix only the exact test-runner failures appended below for WORK_ISSUE_URL on SHARED_BRANCH.

Read the reported command, exit status, and output first. Reproduce or narrow the concrete failure as needed, then make the smallest in-scope implementation change that fixes it. Do not turn this into open-ended review, cleanup, hardening, a refactor, or future work. Run focused or single tests while changing code.

Commit the fix on the existing shared branch and report the exact commit SHA and focused-test results. Do not push, open or manage a pull request, close issues, create subagents, contact agents, run Paseo, or run the full make commands yourself; the existing test runner does that. Never ignore errors.
