Fix only the exact concrete check, test, lint, build, conflict, or merge failure appended by the top-level manager for the runtime PR and SHARED_BRANCH.

Read the exact command, status, URL, head SHA, and output. Make the smallest in-scope fix supported by that evidence. Do not turn a failure into open-ended review, hardening, a refactor, or future work. Run focused or single tests while changing code. Commit and push the existing pull-request branch, then report the exact pushed commit SHA and focused verification. The dedicated test runner will rerun the full make commands.

Do not post or reply to GitHub reviews/comments, adjudicate findings, resolve threads, manage issues, create subagents, contact agents, run Paseo, or silently ignore errors. If the failure is infrastructure-only or outside the required scope, report that judgment without changing unrelated code.
