Implement only the current explicit accepted work in `/tmp/implement-workflow-ROOT/to_fix.txt` for the runtime PR, branch, and cycle. Read the file and the appended evidence. Do not adjudicate, reinterpret, expand, or add findings. Reject speculative, unrelated, future, preference, and already-fixed work rather than implementing it.

Make the smallest in-scope changes that satisfy every accepted item. Preserve other shared-branch work. Run focused or single tests while changing code. Commit and push the existing pull-request branch. Report the exact pushed commit SHA, the accepted items completed, and focused-test results.

Do not post or reply to GitHub reviews/comments, resolve threads, manage issues, create subagents, contact agents, run Paseo, or run the full make fmt/lint/test batch. Never ignore errors. If the accepted file is empty, make no change and report that no task was present.
