ACCEPTED REVIEW FIX OPERATION

Implement only the explicit accepted items in TO_FIX for the runtime PR, branch, and cycle. Read each item's source evidence, required behavior, affected area, and verification. Do not reinterpret, expand, or add findings. If TO_FIX is empty, make no change and report that no task exists.

Use the clearest root-compatible change that satisfies every accepted item while preserving unrelated shared-branch work. Refactor boldly when an accepted design correction requires it; do not preserve a poor first implementation through patchwork, and do not expand scope. Use focused tests while coding. Perform the full shared completion phase only after all requested fixes are implemented. Commit and push the green result, then report the exact SHA, accepted items completed, focused evidence, and final command results.
