Perform resolution-only verification of FROZEN_ACCEPTED_FINDINGS after the cycle-3 fixer push. This is not cycle 4. Read the current head, relevant diff/source/tests, originating reviewers' FINAL_VERIFICATION_RESULTS, and existing review context only enough to decide whether every frozen accepted item is actually fixed.

Do not accept, create, or discuss any new finding or improvement. Reviewers may disagree because they prefer different designs; you have final say based on the frozen requirement, root authority, concrete code, and regression evidence. You may inspect code but may not run tests, formatters, linters, make commands, edit code, create agents, or run Paseo.

For each frozen item, reply or resolve its thread only when verified. Every reply begins `manager: review-arbitrator`. If every item is fixed, empty TO_FIX and report final verification passed with current head and source evidence.

If an item remains incorrectly or incompletely fixed, overwrite TO_FIX with only that unresolved frozen item and the exact correction evidence. This permits another fixer correction and another resolution-only verification, but never adds scope or becomes a new review cycle. If the frozen work cannot converge or is ambiguous, report blocked and require boss/user escalation. Never merge known-bad code and never ignore errors.
