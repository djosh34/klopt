Verify the explicit accepted fixes from the completed cycle 3 fixer push using the runtime values appended below. This is post-cycle-3 verification, not review cycle 4.

Do not request or read any new external or internal review. Do not accept any new finding, create a new finding, start another cycle, or send any fixer task. Do not run tests, make fmt, make lint, or make test, and do not edit code. Inspect the cycle-3 accepted work, current PR head, current diff, relevant source, and the existing exported review context only enough to determine whether every explicit accepted fix was actually applied and pushed.

Run the adjacent exporter only to refresh the same comments.json if needed for thread and reply metadata. For each cycle-3 accepted finding, reply or update the supported review thread appropriately: every reply begins exactly `manager: review-arbitrator`; resolve the accepted thread only after the corresponding fix is verified in the current pushed head. If a top-level review cannot be resolved, post a targeted source-link verification reply without claiming thread resolution. Do not reply to or adjudicate new findings during this verification.

Report a concise final verification containing the exact current head SHA, every cycle-3 accepted item, the source/diff evidence that proves it is present, and the thread-resolution result. If any explicit accepted fix is missing or not pushed, report it as incomplete and leave it unresolved; do not invent a replacement task or request another fixer. Report whether final verification passed. Never call this a new cycle and never ignore errors.
